buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Aapke Gradle 8.10.2 ke liye compatible version
        classpath 'com.android.tools.build:gradle:8.7.2'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}